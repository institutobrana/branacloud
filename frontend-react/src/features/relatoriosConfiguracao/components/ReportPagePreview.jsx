import { effectivePaperDimensions } from '../constants/reportSettingsConstants.js';
export function ReportPagePreview({ config }) { const s = config.sectionStyles?.[config.sectionId] || {}; const meta = [config.printUser && 'Usuário', config.printPage && 'Página 1', config.printDateTime && '03/09/2026'].filter(Boolean).join(' • '); const paper = effectivePaperDimensions(config); const ratio = paper.width / paper.height; return <div className="report-page-preview" style={{ '--paper-ratio': ratio, padding: `${config.marginTopCm * 3}px ${config.marginRightCm * 3}px ${config.marginBottomCm * 3}px ${config.marginLeftCm * 3}px` }}><div className="report-preview-logo" style={{ visibility: config.printLogo ? 'visible' : 'hidden', backgroundImage: config.logoDataUrl ? `url(${config.logoDataUrl})` : 'none' }} /><div>{config.headerText}</div><small>{meta}</small><strong style={{ fontFamily: s.fontFamily, fontSize: s.fontSize, fontWeight: s.bold ? 700 : 400, fontStyle: s.italic ? 'italic' : 'normal', color: s.color, textDecoration: `${s.underline ? 'underline ' : ''}${s.strike ? 'line-through' : ''}`.trim() || 'none' }}>Título do relatório</strong><div className="report-preview-lines" /></div>; }
